One `<moduleId>.md` per module, from `../BRIEF-template.md`.

Written before a diagram is redrawn, not after. The pilot modules come first:
`neuromuscularJunction` (cellular), `cardiorenal` (anatomical), `venousReturn` (graph).
